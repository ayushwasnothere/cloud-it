import { LoginForm } from '@/components/auth/LoginForm'

export const metadata = {
  title: 'Sign In - Cloud IDE',
  description: 'Sign in to your Cloud IDE account',
}

export default function LoginPage() {
  return <LoginForm />
}
